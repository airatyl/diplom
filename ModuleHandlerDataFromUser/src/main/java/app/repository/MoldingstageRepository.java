package app.repository;

import app.entity.Moldingstage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MoldingstageRepository extends JpaRepository<Moldingstage, Integer> {
}