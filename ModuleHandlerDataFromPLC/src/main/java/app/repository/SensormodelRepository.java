package app.repository;

import app.entity.Sensormodel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SensormodelRepository extends JpaRepository<Sensormodel, Integer> {
}